SSH 
ssh -i .\tomboloqa-node-vm001_key.pem azureuser@52.255.169.198

docker ps -a

--Building
1.navigate to the dir - cd /opt/Projects/Auth-Service
2. git pull
3. docker-compose up -d --no-deps --build node web
4. if you get error at 3 run - sudo chown -R asureuser mysql-data and re-run 3

--Reading logs
1. SHH
2. docker pa -a
3. go inside tombolo_node - docker exec -it 64d sh
4.pm2 logs --lines 200


#### 
ssh -i .\tomboloqa-node-vm001_key.pem azureuser@52.255.169.198
tombolo_node - docker exec -it 64d sh
cd /opt/Projects/Tombolo/
git pull
docker-compose up -d --no-deps --build web node

