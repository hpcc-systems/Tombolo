import React from 'react';

const ClusterGuide: React.FC = () => (
  <div className="guide">
    <h2>Cluster Guide</h2>
    <p>
      This guide covers cluster monitoring essentials: nodes, services, health indicators, and recommended
      troubleshooting steps for common cluster issues.
    </p>
  </div>
);

export default ClusterGuide;
