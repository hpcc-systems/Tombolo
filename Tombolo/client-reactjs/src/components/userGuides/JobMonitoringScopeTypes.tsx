import React from 'react';

const JobMonitoringScopeTypes: React.FC = () => (
  <div className="guide">
    <h2>Job Monitoring Scope Types</h2>
    <p>
      Explains the different scope types used for job monitoring (e.g., cluster-wide, application-scoped, and
      user-scoped) and when to use each.
    </p>
  </div>
);

export default JobMonitoringScopeTypes;
