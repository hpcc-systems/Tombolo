// Re-exported from @tombolo/shared/backend so both server and jobs can consume this.
export { msGraphClient, extractFirstName } from '@tombolo/shared/backend';
export type {
  MSGraphUser,
  UserWithManager,
  MSGraphManager,
} from '@tombolo/shared/backend';
