<!DOCTYPE html>
<html>

<head>
  <style>
    .main_body {
      width: 100%;
      border-bottom: 1px solid #f0f0f0 !important;
    }

    .section-footer-td {
      color: #808080;
    }

    .section-header-td {
      padding-top: 8px;
      padding-bottom: 8px;
    }
  </style>
</head>

<body>
  <table class="main_body">
    <tbody>
      <tr>
        <td>
          <table>
            <tbody>
              <tr>
                <td>
                  Hello,
                </td>
              </tr>
              <tr>
                <td class="section-header-td">
                 A user whose password has expired is requesting their password be unlocked by resetting their password. Details of the user are as follows:
                </td>
              </tr>
              <tr>
                <td class="section-header-td">
                  <table>
                    <tr>
                      <td>
                        User email: <%= typeof userEmail !== 'undefined' ? userEmail : '' %>
                      </td>
                      <td>
                        User name: <%= typeof userName !== 'undefined' ? userName : '' %>
                      </td>
                    </tr>
                  </table>
                </td>
              </tr>
              <tr>
                <td class="section-header-td">
                    Please follow the link below to the user management page to reset the user's password. 
                    <br/><br/>
                    <a href=<%= typeof passwordResetLink !== 'undefined' ? passwordResetLink : '' %>><%= typeof passwordResetLink !== 'undefined' ? passwordResetLink : '' %></a>
                </td>
              </tr>
              <tr>
                <td class="section-header-td">
                  <div>Regards,</div>
                  <div>Tombolo</div>
                </td>
              </tr>
            </tbody>
          </table>
        </td>
      </tr>
    </tbody>
  </table>
</body>

</html>