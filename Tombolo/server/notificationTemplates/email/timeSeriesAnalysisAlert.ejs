<!DOCTYPE html>
<html>

<head>
  <style>
    /* Keep only the styles that must be here */
    .boldText {
      font-weight: bold;
      font-size: 1rem;
    }
  </style>
</head>

<body>
  <table width="100%">
    <tbody>
      <tr>
        <td>
          <table width="1280" cellpadding="0" cellspacing="0" style="font-family: Arial, sans-serif; font-size: 14px; margin-top: 1rem;  margin-bottom: 1rem;">
            <tbody>
              <tr>
                <td style="margin-bottom: 30px;">
                  Time Series Analysis has detected a significant deviation in the work unit <%= Wuid %> for this job compared to expected metrics. 
                  <br/>
                  <br/>
                  This anamoly was detected on <%= date %> GMT.
                </td>
              </tr>
            </tbody>
          </table>

      </tr>
    </tbody>
  </table>
  </td>
  </tr>
  </tbody>
  </table>

  <table width="1080" cellpadding="5" cellspacing="0" border="1" style="border-collapse: collapse;">
    <tbody>
      <tr style="background-color: #FFBF00; font-weight: bold;">
        <td>Metric</td>
        <td><%= Wuid %> Value</td>
        <td>Expected Value Range (Last <%= lastRunsLength %>)</td>
        <td>Delta</td>
        <td>Z-Score</td>
        <% for(let i=0; i < lastRunsLength; i++) { %>
          <td> Run <%= i + 1 %></td>
        <% } %>
      </tr>

      <% alertPoints.forEach((point, index) => { %>
      <tr>
        <td><%= point.name %></td>
        <td><%= point.current %></td>
        <td><%= point.expectedMin %> - <%= point.expectedMax %></td>
        <td><%= point.delta %></td>
        <td><%= point.zScore %></td>
        <% point.historical.forEach((run) => { %>
          <td><%= run %></td>
        <% }); %>
      </tr>
      <% }); %>

    </tbody>
  </table>

  <table width="1080" cellpadding="0" cellspacing="0">
    <tbody>
      <tr>
        <td style="padding-top: 30px;">
          <div style="font-weight: bold;">
            Click the link below to view detailed analysis results and run history.
            <br/>
            <br/>
            <a href="<%= link %>">View Time Series Analysis</a>
          </div>
        </td>
      </tr>
    </tbody>
  </table>
  </td>
  </tr>
  </tbody>
  </table>
</body>

</html>