apiVersion: v1
kind: ConfigMap
metadata:
  name: tombolo-server-config
  namespace: {{ include "tombolo.namespace" . }}
data:
{{- range $key, $value := .Values.server.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
