export enum hpccToolsJobType {
  // eslint-disable-next-line
  SYNC = 'sync',
}

export enum wuHistoryJobType {
  // eslint-disable-next-line
  QUERY = 'query',
  // eslint-disable-next-line
  DETAILS = 'details',
  // eslint-disable-next-line
  INFO = 'info',
}
