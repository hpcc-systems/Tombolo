{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "dev": {
      "description": "Runs all packages in the repo in dev mode with hot reloading",
      "cache": false,
      "persistent": true,
      "dependsOn": ["^esm:build", "^db:migrate", "^db:seed"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "dist/**", ".vite/**"],
      "inputs": [
        "src/**",
        "public/**",
        "*.config.js",
        "*.config.ts",
        "package.json"
      ],
      "env": ["NODE_ENV", "VITE_*"]
    },
    "test": {
      "cache": false,
      "outputs": ["coverage/**"]
    },
    "lint": {
      "cache": true,
      "outputs": []
    },
    "lint:fix": {
      "cache": false,
      "outputs": []
    },
    "db:create": {
      "cache": false
    },
    "db:migrate": {
      "description": "Runs db migrations with sequelize",
      "cache": false,
      "dependsOn": ["^esm:build"]
    },
    "db:seed": {
      "description": "Seeds the database with sequelize",
      "cache": false,
      "dependsOn": ["db:migrate"]
    },
    "db:drop": {
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "esm:build": {
      "description": "transpiles the db, core and shared package for use in dev mode",
      "dependsOn": ["^esm:build"],
      "cache": true,
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    }
  }
}
